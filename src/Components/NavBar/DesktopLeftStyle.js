import { styled } from 'reuse/Packages';

const StyledLeftWrapper = styled.span`
  display: flexbox;
  flex-direction: row;
  width: 30rem;
  justify-content: space-between;
  padding-right: 15rem;
  z-index: 1;
  position: relative;
`;

export default StyledLeftWrapper;
